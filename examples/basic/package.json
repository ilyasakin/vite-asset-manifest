{
  "name": "basic-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "End-to-end smoke test for vite-asset-manifest. Builds a small Vite app that exercises every interesting code path in the plugin.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite-asset-manifest": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^8.0.0"
  }
}
